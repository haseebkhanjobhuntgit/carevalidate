type Post {
  id: ID!
  title: String!
  slug: String!
  content: String!
  published: Boolean!
  createdAt: String!
  updatedAt: String! 
  
  author: Author!
  categories: [Category!]!
  tags: [Tag!]!
}

type Author {
  id: ID!
  email: String!
  name: String
  bio: String
  posts: [Post!]!
}

type Category {
  id: ID!
  name: String!
  slug: String!
  posts: [Post!]!
}

type Tag {
  id: ID!
  name: String!
  slug: String!
  posts: [Post!]!
}

input PostFilter {
  search: String
  published: Boolean
  authorEmail: String
  categorySlugs: [String!]
  tagSlugs: [String!]
}

type Query {
  posts(filter: PostFilter): [Post!]!
  
  post(slug: String!): Post
  
  author(id: ID, email: String): Author
  
  categories: [Category!]!

  tags: [Tag!]!
}

